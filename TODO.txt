TODO
- filtering conversations by topic, status
- pagination for conversations and messages
- add timestamps to messages and conversations


# Operator accessing all conversations
curl -u alex:operator123 http://localhost:8080/conversations

# Customer creating a conversation
curl -u john:password123 -X POST http://localhost:8080/conversations \
  -H "Content-Type: application/json" \
  -d '{"customerId": 1, "topic": "TECHNICAL", "initialMessage": "Help needed"}'

# Operator taking over a conversation
curl -u lisa:operator123 -X POST http://localhost:8080/conversations/2/takeover \
  -H "Content-Type: application/json" \
  -d '{"operatorId": 6}'



  # John (customer) creating a conversation
curl -u john:password123 -X POST http://localhost:8080/conversations \
  -H "Content-Type: application/json" \
  -d '{"customerId": 1, "topic": "TECHNICAL", "initialMessage": "Help needed"}'


  # Alex (operator) viewing all conversations
curl -u alex:operator123 http://localhost:8080/conversations

# Lisa (operator) taking over a conversation
curl -u lisa:operator123 -X POST http://localhost:8080/conversations/2/takeover \
  -H "Content-Type: application/json" \
  -d '{"operatorId": 6}'




  curl -v -X POST http://localhost:8080/conversations \
  -H "Content-Type: application/json" \
  -u john:password123 \
  -d '{
    "topic": "SERVICES",
    "initialMessage": " "
  }'


    curl -v -X POST http://localhost:8080/conversations \
  -H "Content-Type: application/json" \
  -u john:password123 \
  -d '{
    "topic": "SERVICES",
    "initialMessage": "test"
  }'